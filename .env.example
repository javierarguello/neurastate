# Database Connection
# Use individual variables to avoid URL encoding issues with special characters
DB_INSTANCE_HOST=your-database-host
DB_USER=postgres
DB_PASS=your-password
DB_NAME=neurastate
# Optional: defaults to 5432 if not specified
# DB_PORT=5432

# Optional: Set to 0 to bypass TLS certificate validation (development only)
NODE_TLS_REJECT_UNAUTHORIZED=0

# Google Cloud
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: External Services
# EMAIL_API_KEY=
# SMS_API_KEY=
